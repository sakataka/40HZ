# AGENTS.md instructions for /Users/sakataka/40Hz

<INSTRUCTIONS>
・回答は日本語で行う
・Codex は、明示依頼がない限り formatter / format check / import sort の新規導入・設定変更や、フォーマットだけを目的とした実行をしない。既存プロジェクトの標準的な検証に含まれるものは実行してよい
・このプロジェクトで機能追加、UI変更、バグ修正、テスト修正など、ある程度まとまった変更を行った場合は、原則として検証後にコミットしてリモートへ push まで行う
・コード・依存関係・ビルド設定を変更した場合は、コミット前に `bun run test` と `bun run build` を通す。文書・指示文だけで実行成果物に影響しない変更は、記述・参照先・差分を確認する
・小さな文言修正も含め、今回の依頼で変更した差分だけをコミット/pushする。調査のみで変更がない場合はコミットしない
・コミットメッセージは変更内容が分かる短い英語にする
・既存の未コミット変更がある場合は、内容を確認し、今回の作業と無関係な変更を勝手に戻さない
</INSTRUCTIONS>
